namespace beatmaker.audio;

public interface IAudioPlayer {
  // handle playing backing track, hit sounds, and instrumentals
  // arbitrary - pass in some generic data telling the "engine wrappers" what to do
}